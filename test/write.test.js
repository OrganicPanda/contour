var elevation = require('../assets/scripts/elevation')
  , assert = require('assert');

describe('elevation', function () {
  // var div;

  beforeEach(function () {
    // div = document.createElement('div');
    // div.id = 'container';
    // document.body.appendChild(div);
  });

  afterEach(function () {
    // div.parentElement.removeChild(div);
  });

  it('Adds an h2 to container', function () {
    assert(true, 'el exists');
  });
});
